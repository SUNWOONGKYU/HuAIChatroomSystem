-- 산출물을 폰에서 열 수 있는 주소.
--
-- uri 는 이 PC 안의 경로다(`file:///C:/...`). 방장은 폰으로 보므로 그 값으로는 아무것도
-- 열리지 않는다 — 현황판에 "결과물"이 떠도 눌리지 않는 상태였다.
--
-- uri 를 덮어쓰지 않고 컬럼을 따로 두는 이유: uri 는 이 파일이 어느 작업 폴더의
-- 무엇이었는지를 가리키는 사실이고, 감사·추적이 그걸 쓴다. 공개 주소는 그것과 별개로
-- "지금 열어볼 수 있는 곳"이며, 다시 배포하면 바뀔 수 있다.

alter table huai_artifacts
  add column if not exists public_url text;
