━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  MBO 목표서 — 다방 지원 + /tasks 개선 + Telegram Mini App 작업판
  (rev.2 — 2026-08-15, 성능 요구·비교우위·버즈랩 자산 이식 반영)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 최상위 목표

"봇 프로세스 하나로 **뜨문뜨문 20개 방**을 운영하고 그중 **3개 방이 동시에 일해도 밀리지 않으며**, 작업 현황을 표·진행률로 한눈에 보고, 승인은 Telegram 안에서 그대로 처리한다 — 새 앱 설치·앱스토어 등록 없이."

## 성능 요구 (PO 지정, 2026-08-15)

| 사용 패턴 | 요구 | 현재 실측·추정 | 판정 |
|---|---|---|---|
| 뜨문뜨문 사용 (방마다 시간대 다름) | **20방** | 라우팅만 구현 시 10~20방 (경계) | 보강 필요 |
| 동시 사용 (여러 방이 같은 시각에) | **3방** | 라우팅만 구현 시 2~3방, 3방일 때 응답 **4.3분** | **불가 — 범위 확대 필요** |

## 현재 vs 목표

| 항목 | 현재 (AS-IS) | 목표 (TO-BE) |
|------|-------------|-------------|
| 방 지원 | `BOT_SERVICE_ROOM_ID` 환경변수로 프로세스당 방 1개 고정 (`supabase-store.ts:745`, `index.ts:97` allowedChatIds 단일) | chat_id → room_id 런타임 조회, 프로세스 1개로 다방 동시 처리 |
| **실행 동시성** | **1 (하드)** — `local-gateway/src/consumer.ts:42` `for...of await`, 세마포어·워커풀 저장소 전체에 없음. 프로세스 복제도 `start-operation-services.mjs:69`가 차단 | **3 이상** — 워커 풀 + lease 정합성 확보 |
| **outbox 공평성** | **없음** — `schema.sql:328` `order by created_at asc` 전역 FIFO. 한 방이 최대 75분(5행×900초) 전체 점유 | 방 단위 공평성 (방당 in-flight 상한 또는 라운드로빈) |
| **룸 필터 누락** | 2곳이 `room_id` 없이 전역 윈도우 조회 — 다방화 시 **무증상 오작동** | 필터 추가 |
| 작업 현황 조회 | `/tasks` 텍스트 나열, 상태·경과시간·담당 구분 없음 (`orchestrator/index.ts:532`) | 상태별 그룹 + 경과시간 + 담당자 표시 |
| 큰 화면·표·대시보드 | 불가 (Telegram 텍스트 한계) | Telegram Mini App으로 칸반·진행률·이력 제공 |
| DB 보안 | **RLS 정책 0건** (`CREATE POLICY` 전수 검색 0 matches) | Mini App 창구 한정 RLS 신설 |
| 승인 흐름 | Telegram 버튼 그대로 (유지) | 그대로 유지 — 알림 반응성 손실 없음 |
| 설치 부담 | 0 (Telegram 이미 사용 중) | 0 유지 — 새 앱 설치·스토어 심사 없음 |

## 측정 지표 (KPI)

| 지표 | 현재값 | 목표값 | 측정 방법 |
|------|--------|--------|----------|
| 뜨문뜨문 운영 방 수 (프로세스 1개) | 1 | **20** | 방 20개 시딩 후 순차 /tasks 호출 전건 정상 응답 |
| 동시 사용 방 수 | 1 | **3** | 방 3개에서 같은 시각 소대장 호출 → 전건 응답 |
| 동시 3방 소대장 응답 지연 | 해당없음 | **3분 이내** | 3방 동시 호출 시각 ~ 마지막 응답 도착 시각 |
| 실행 동시성 | 1 | 3 이상 | 게이트웨이 로그에서 in-flight 실행 수 동시 관측 |
| 한 방의 전체 점유 방지 | 미보장 (최대 75분) | 다른 방 응답이 **5분 이내** 시작 | 방 A에 장시간 작업 투입 중 방 B 호출 |
| /tasks 가독성 | 평문 나열 | 상태별 그룹+경과시간 표시 | 실제 출력 캡처 비교 |
| Mini App 클릭 성공률 | 해당없음(미존재) | 주요 버튼(작업판 열기·상세보기·승인) 100% 정상 동작 | 실제 Telegram 클라이언트 클릭 테스트 |
| RLS 정책 수 | 0 | Mini App 창구 커버 (본체 서비스롤 경로 무변경) | 정책 목록 조회 + 기존 경로 회귀 테스트 |
| 신규 설치 요구 | 없음 | 없음 (유지) | Mini App이 Telegram 내부에서 열리는지 확인 |

## 산출물 (Deliverable)

| 산출물 | 형태 / 위치 |
|--------|------------|
| 다방 지원 코드 변경 | `apps/bot-service/src/*`, `scripts/*` (diff) |
| 실행 동시성 워커 풀 | `apps/local-gateway/src/consumer.ts`, `runtime.ts` + lease 설정 (diff) |
| outbox 방 단위 공평성 | `supabase/migrations/*` (`lease_huai_outbox` 개정) |
| 룸 필터 누락 2곳 수정 | `supabase-store.ts`, `packages/supabase-runtime/src/index.ts` (diff) |
| /tasks 개선 코드 변경 | `packages/orchestrator/src/index.ts`, `apps/bot-service/src/supabase-store.ts` (diff) |
| 다방 회귀 검증 스크립트 | `scripts/verify-multiroom.mjs` (신규) — 원본 기획서에 없던 항목 |
| Mini App 정적 페이지 | 공개 URL 1개 (호스팅 방식은 실행 단계에서 확정) |
| Mini App 데이터 연동 | Supabase RLS 정책 + Edge Function (initData 서명검증 창구) |

## 버즈랩(buzzlab-nextjs) 자산 이식

PO 지시: "버즈랩에서 필요한 소스코드를 가져다 써야 될 것이 있으면 가져다 쓴다."
주력 저장소는 `C:\Dev\buzzlab-nextjs` (`C:\Dev\BuzzLab`은 TimesFM 실험용 사이드 폴더 — 재사용 자산 없음).

| 우선 | 이식 대상 | 출처 (buzzlab-nextjs) | 판정 | 의존성 |
|---|---|---|---|---|
| 1 | **미니앱 전용 additive-only RLS 전략** — 본체 테이블·정책 무변경, 쓰기는 service role 전용, 읽기만 정책 부여, 롤백문 명시 | `supabase/migrations/069_mini_predictions.sql:1-38` | 설계 패턴 그대로 채택. 본 목표서 리스크 대응책과 동일 전략 | 없음 (순수 SQL) |
| 1 | **소유권 체인 RLS 템플릿** (1~4단계 조인 정책) | `supabase/migrations/002_rls_policies.sql:30-48,53-75,101-115` | 테이블명 치환 후 사용 — HuAI는 `huai_tasks → huai_rooms → huai_room_members` 체인으로 동형 | 없음 |
| 1 | **재귀 없는 admin 정책** (`auth.jwt()->'app_metadata'` 사용, profiles 재조회 금지) | `019_rls_billing_snapshots_logs_contributions.sql:26-38` | 그대로 | 없음 |
| 1 | **RLS 무한재귀(42P17) 회피 지식 + 컬럼단위 GRANT 잠금** | `024_fix_profiles_rls_recursion.sql`, `062_profiles_security_lockdown.sql:11-19` | 함정 회피용 참고 — HuAI가 `room_members` 참조 정책 작성 시 밟게 될 함정 | 없음 |
| 2 | **진행률·상태 렌더링 컴포넌트** — STAGE_META 선언표, 진행률 바, 완료/진행/대기 3상태 카드, 수치 요약 바 | `src/components/simulation/StageProgressBar.tsx:5-11,40-51,55-104,108-120` | 배열 내용만 HuAI 작업 상태로 교체 | Tailwind + lucide-react |
| 2 | **상태 배지 사전 + 필터 칩 + 스켈레톤** | `src/app/(pages)/simulations/page.tsx:20-57,108-119,131-162` | 라벨만 교체, 색상 팔레트 채택 | Tailwind + lucide-react |
| 2 | **목록형 운영 도구 골격** (fetch→행별 액션→낙관적 업데이트→per-row busy 락) | `src/app/(pages)/admin/mini/page.tsx:31-134` | 작업판 참조 구현 (135줄 자기완결) | React 19, lucide-react |
| 3 | **상수시간 토큰 비교 함수** (양측 SHA-256 후 XOR — 길이 조기반환 없음) | `supabase/functions/decrypt-api-key/index.ts:11-25` | initData 검증 최종 hash 비교에 그대로 투입 | WebCrypto만 (의존성 0) |
| 3 | **Edge Function 보일러플레이트** (Deno.serve + CORS 프리플라이트 + service_role 클라이언트) | `supabase/functions/decrypt-api-key/index.ts:1-102` | 골격만 참고 (하는 일은 무관) | Deno, esm.sh supabase-js |

**버즈랩에서 가져올 수 없는 것 — 신규 작성 확정**

| 항목 | 확인 |
|---|---|
| Telegram initData HMAC-SHA256 서명검증 | 버즈랩 `createHmac`/`crypto.subtle.sign`/`HMAC` 전수 검색 **0건** |
| chat_id → room_id 런타임 라우팅 | 버즈랩은 멀티테넌트 개념 자체 없음 (`002_rls_policies.sql:14-15` 단일 소유 체인). 참고할 것조차 없음 |
| 칸반 보드 | `kanban`/`칸반` 검색 **0건** |
| 인간 승인 워크플로우 | 승인 게이트 부재 — AI 자율 실행 후 결과 리포트만 |

## 비교 우위 (vs 버즈랩)

**HuAI가 앞서는 점**

| 항목 | HuAI | 버즈랩 | 근거 |
|---|---|---|---|
| 설치 부담 | Telegram 봇 — 이미 쓰는 앱 안에서 완결 | 웹 로그인 필수, 모바일은 Expo 네이티브 앱 **별도 설치 + 스토어 심사** | `buzzlab-mini/app.json:11-14` |
| 다자 협업 모델 | `huai_rooms` + `huai_room_members(room_id, telegram_user_id)` + `unique(room_id, role)` — 다자 협업이 스키마 1급 개념 | **단일 사용자 소유 모델뿐**, 프로젝트 공유 개념 없음 | HuAI `schema.sql:19-43` vs `002_rls_policies.sql:14-15` |
| 인간 승인 게이트 | 승인 워크플로우가 제품 축, append-only 승인 이력 | 없음 | `20260814220000_huai_approvals_entity_ref_and_append_only.sql` |
| 신뢰성 전달 | outbox 패턴 + 중복 재처리 가드 + 멱등성 | outbox 없음, cron 폴링 재개(1분) | `20260810112000_huai_outbox_processing_guards.sql` vs `vercel.json:29-31` |
| 실시간 양방향 | 채팅방 안에서 즉시 지시·즉시 승인 | 웹 폼 제출 후 최대 300초 대기 | `vercel.json:7-22` |

**버즈랩이 앞서는 점 (정직하게 — 이번 작업의 이식 근거)**

| 항목 | 버즈랩 | HuAI | 근거 |
|---|---|---|---|
| DB 보안 | `CREATE POLICY` **89건** / 22개 마이그, 소유권 체인 4단계, 재귀회피, 컬럼단위 GRANT, 라이브 취약점 대응 이력 | **0건** | 전수 검색 |
| 웹 UI·시각화 | dashboard 457줄, admin 994줄, D3 시각화 3종, shadcn/ui 세트 | 없음 (Telegram 텍스트만) | `src/components/visualization/` |
| 프로덕션 운영 | Vercel(icn1) + 커스텀 도메인 + Sentry + Analytics + 5분 헬스체크 cron + 롤백 절차 문서 | 로컬 게이트웨이 + 봇 프로세스 | `docs/ops-deployment.md:141-155`, `vercel.json:24-32` |

**요약 문장**: 버즈랩은 웹·앱 채널의 단일 사용자 소유 SaaS로 다자 협업·인간 승인 구조가 없다. HuAI는 반대로 다자 협업 방 + 인간 승인 게이트 + 설치 부담 0의 Telegram 채널을 축으로 삼는다. 다만 RLS(89 대 0)·웹 UI·프로덕션 운영은 버즈랩이 명확히 앞서며, 이번 작업은 그 격차를 버즈랩 자산 이식으로 메운다.

## 완료·검증 기준

| 완료 조건 | 검증 방법 | 검증 주체 |
|----------|----------|----------|
| Phase 0: 스키마-코드 정합성 복구 | 시드 스크립트 실행 성공 + 봇 조회 정상 | V1 |
| Phase 1: 다방 지원 실동작 | 방 2개에서 각각 /tasks·승인버튼 실제 호출 | V1(별도 세션) |
| Phase 2: 동시성 요구 충족 | **방 3개 동시 호출 → 전건 3분 이내 응답** | V1 + 실측 |
| Phase 2: 뜨문뜨문 20방 | 방 20개 시딩 후 순차 호출 전건 정상 | V1 + 실측 |
| Phase 3: /tasks 가독성 개선 | 실제 출력 캡처 확인 | V1 |
| Phase 4: Mini App 버튼 100% 동작 | 실제 Telegram 클라이언트 클릭 테스트 (curl 200만으론 불인정) | 별도 Verification Agent |
| 전체 | pnpm typecheck + `verify:*` + 신규 `verify-multiroom` 통과 | V1 → 소대장 통합 → V2(Codex) |

## 실행 계획

| # | Phase | 설명 | 예상 결과 |
|---|------|------|----------|
| 0 | 정합성 복구 | 미커밋 스키마 변경과 코드 어긋남 해소 (아래 리스크 R1) | 시드·봇 조회 정상 |
| 1 | 다방 라우팅 | store+routing+authorization+execution defaults 다방화 + 룸 필터 누락 2곳 | 방 2개 이상 동시 실동작 |
| 2 | 동시성 확보 | 게이트웨이 워커 풀(동시 3+) + lease 정합성 + outbox 방 단위 공평성 | 동시 3방 3분 이내 응답 |
| 3 | /tasks 개선 | 상태별 그룹+경과시간+담당자 | 가독성 있는 출력 |
| 4 | Mini App | 호스팅 확정 → 버즈랩 RLS 전략 이식 → initData 검증 Edge Function 신규 작성 → 정적 페이지(버즈랩 컴포넌트 이식) → BotFather 등록 | 작업판 실사용 가능 |

## 리스크

| # | 리스크 | 대응 방안 |
|---|--------|----------|
| R1 | **미커밋 스키마 변경이 코드보다 앞서 나가 이미 깨져 있음** — ① `generate-supabase-room-seed.mjs:37` insert에 `role` 없음, 마이그레이션이 NOT NULL로 만듦 → 지금 실행하면 실패. ② `supabase-runtime-loader.ts:136-139`가 여전히 `actor_id`로 봇 조회, 마이그레이션 의도와 반대 → 2번째 방에서 봇 0건, 모든 메시지 무시 | Phase 0에서 코드 동반 수정 후 마이그레이션 적용. **라이브 DB(방 1개 active, task 13건 실데이터)이므로 적용 전 백업 필수** |
| R2 | **룸 필터 누락 2곳이 무증상으로 깨진다** — `supabase-store.ts:467`(proposal_created limit 200, room 조건 없음) → 방 늘면 승인 눌러도 `approved-task-materialization-missing`. `packages/supabase-runtime/src/index.ts:533`(meaningful_intermediate_ready limit 100, room 조건 없음) → 멀티AI 교차감사가 에러 없이 영영 안 돎 | 다방화와 **동시에** 수정. 별도 Phase로 미루지 않는다 |
| R3 | **lease(16분) < 배치 최악(75분)** — 워커를 늘리는 순간 `schema.sql:325` `status='processing' and locked_until < now()`가 발동해 **같은 CLI 명령 중복 실행**. `20260814210000_huai_artifacts_unique_and_guards.sql:2-5`가 이미 이 현상을 인지·기록했으나 아티팩트 중복 삽입만 막았을 뿐 CLI 재실행은 못 막음 | Phase 2에서 워커 풀과 lease 개정을 **한 묶음으로** 처리. `LEASE_MS ≥ MAX_RUNTIME_MS × LIMIT` 또는 행 단위 리스로 전환 |
| R4 | 폴링 병렬화 시 봇 선점 경합 붕괴 — `schema.sql:84-86` unique 인덱스로 "먼저 기록한 봇이 이기는" 구조인데, 현재 승자가 `server.ts:115-120` 배열 순서(platoon 우선)에 우연히 의존. 병렬화하면 소대장 멘션이 CodexBot에 먹힐 수 있음 | 폴링 병렬화는 경합 해소 후에만. Phase 2 목표(3분)는 워커 풀만으로 달성 가능하므로 **이번 범위에서 제외 판단 가능** |
| R5 | 429 5회면 메시지 영구 소실 — `schema.sql:334` `attempts`가 리스마다 증가, `outbox.ts:143` 5회면 markDead. 선제 throttle 없음(`throttle`/`rate limit` 실코드 0건). DLQ 재투입 경로 없음(수동 스크립트만) | 방 20개로 늘면 발현 확률 상승. Phase 2에서 429를 `attempts`에서 제외하는 최소 수정 포함 |
| R6 | 기존 단일방 운영(`BOT_SERVICE_ROOM_ID` 세팅된 환경) 회귀 | chat_id 조회 방식 전환 시 하위호환 유지 여부를 분대장이 코드 일관성 기준으로 판단 |
| R7 | Mini App 호스팅 방식이 웹훅 인프라 재확인 결과에 따라 바뀜 | Phase 4 착수 시점에 재확인 후 확정 — PO 재보고 없이 소대장 판단으로 진행 |
| R8 | RLS 신설이 기존 서비스롤 경로에 영향 | 버즈랩 `069` 전략 채택 — Mini App 전용 창구 한정, 본체 정책 무변경, 롤백문 동봉 |

## 원본 기획서 대비 추가된 것 (근거)

원본 `2026_08_11__CODEX_방식A_다중그룹_온보딩_지시문.md`가 놓쳐서 구현이 막혔던 항목:

| 원본 결함 | 본 목표서 반영 |
|---|---|
| "기존 테이블 재사용" 전제가 물리적으로 거짓 (`actor_id NOT NULL UNIQUE` × `unique(room_id, role)` 조합이 봇 공유를 원천 차단) | Phase 0에 스키마 변경 명시 |
| `BOT_SERVICE_ROOM_ID`를 **0회 언급** — 최대 장애물에 지시 전무 | AS-IS 표에 명시, Phase 1 핵심 |
| 검증 명령 8개 전부 단일방 — 다방 회귀를 잡을 수단 0 | `verify-multiroom.mjs` 산출물 추가 |
| outbox room 격리를 완료 조건에 미포함 | R2·Phase 2에 반영 |
| §8(username 리터럴 라우팅) vs §9(username 판정 금지) 자기모순 | 잔존 하드코딩 인지 (`orchestrator/index.ts:653-655,668-669`) |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
이 목표를 승인하시겠습니까?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
