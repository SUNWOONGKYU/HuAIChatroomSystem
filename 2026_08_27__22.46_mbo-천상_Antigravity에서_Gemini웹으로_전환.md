# MBO 목표서 — Antigravity CLI에서 Gemini 웹으로 전환

승인 일시: 2026-08-27

## 최상위 목표

사용량 한도가 낮은 Antigravity CLI 실행 경로를 Gemini 웹 세션 자동화로 교체하고, Claude Code와 Codex 실행 경로는 유지한다.

## 현재 vs 목표

| 항목 | 현재 (AS-IS) | 목표 (TO-BE) |
|---|---|---|
| Antigravity 작업 경로 | `antigravity` / `agy` CLI | Gemini 웹 + 전용 Chrome CDP |
| Claude Code | 유지 | 유지 |
| Codex | 유지 | 유지 |
| Gemini 인증 | 미연결 가능성 | 전용 자동화 Chrome 세션 재사용 |
| 실패·한도 대응 | Antigravity 한도 의존 | Gemini 세션·로그인·타임아웃 오류를 명시 처리 |

## KPI

| 지표 | 목표값 | 측정 방법 |
|---|---:|---|
| Antigravity 실행 요청의 Gemini 라우팅 | 100% | 어댑터·라우터 테스트 |
| Claude/Codex 기존 경로 회귀 | 0건 | 기존 어댑터 테스트 |
| Gemini 웹 신규 응답 검증 | 100% | 응답 기준선·안정화 폴링 테스트 |
| 로그인·CDP·타임아웃 오류 분류 | 100% | 실패 시나리오 테스트 |
| 브라우저 세션 중복 실행 | 0건 | CDP 연결·동시성 테스트 |

## 산출물

| 산출물 | 형태 / 위치 |
|---|---|
| Gemini 웹 실행 어댑터 | `packages/ai-adapters/` 및 gateway |
| Antigravity→Gemini 타입·라우팅 전환 | contracts·runtime·DB migration 코드 |
| Gemini 세션 오류 처리 | 웹세션 자동화 연동 코드·테스트 |
| 운영 환경 설정 갱신 | `.env.operation.example`·문서 |
| 독립 검증 보고서 | 검증 결과 |

## 실행 계획

1. Antigravity 참조·호출자·DB 계약·fallback 전수 조사
2. `gemini_web` 어댑터를 확장 경로로 추가
3. 웹세션 자동화 `session.js chat --site gemini` 연결
4. Antigravity 요청을 Gemini로 전환하고 Claude/Codex 회귀 검증
5. 로그인·CDP·응답 기준선·타임아웃·동시성 처리
6. 독립 검증 후 Antigravity 실행 경로 비활성화·제거
7. 운영 문서와 환경 템플릿 갱신

## 완료·검증 기준

- Antigravity CLI가 실제 실행되지 않는다.
- Antigravity 대상 작업이 Gemini 웹으로 라우팅된다.
- Gemini의 새 응답만 결과로 인정한다.
- Gemini 로그인·CDP·세션 오류가 작업 실패로 명확히 기록된다.
- Claude Code와 Codex 작업은 기존처럼 실행된다.
- 타입체크·빌드·관련 테스트와 독립 검증을 통과한다.

## 제약

- 자격증명 입력은 하지 않는다. Gemini 로그인은 기존 전용 Chrome 세션만 재사용한다.
- 서버 작업자만 코드를 수정한다.
- 외부 운영 DB 적용·서비스 재시작·Antigravity 최종 제거는 별도 확인 후 진행한다.

## 결과 보고

### 판정

**정적·로컬 검증 범위에서 목표 달성.** Antigravity CLI 실행 호출은 제거되었고, 레거시 값은 읽기 호환·Gemini 정규화 용도로만 남겼다.

### 검증 결과

- `antigravity` → `gemini_web` 정규화: 통과
- contracts → runtime → gateway → process runner → 결과 수집: 통과
- Gemini 신규 응답·CDP·로그인·제출·타임아웃 분류: 통과
- Claude/Codex 회귀: 통과
- TypeScript typecheck·build: 통과
- 관련 테스트: 188개 통과
- migration·env·schema 정의 일치: 통과

### 운영 전 남은 조치

1. 대상 Supabase에 migration 적용
2. 운영 env와 전용 Chrome CDP 9222·Gemini 로그인 세션 설정
3. 실제 Gemini 1회 전송 및 브라우저 user-journey 검증
4. 서비스 재시작 후 운영 smoke test

실제 Gemini 전송·외부 DB 적용·서비스 재시작은 완료했다. 새 Gemini 대화에서 `GEMINI_WEB_OK` 응답을 확인했고, 운영 migration 적용·서비스 재시작·운영 상태 `ok`를 확인했다.

문서 대조 기준을 갱신했다. `GEMINI_WEB_SESSION_SCRIPT` 기반 전용 Chrome CDP 세션을 사용하며, Antigravity/agy는 사용량 한계·호환성 때문에 `gemini_web`로 읽기 호환한다. Gemini 웹은 응답 전용이고 파일 수정은 Claude Code·Codex가 담당한다. synthetic LeaderBot 계획 webhook 테스트는 가짜 transport/fetch로만 실행한다.

## 게임만들기 최종 테스트 기록

| 단계 | 담당 | 결과 | 증거 |
|---|---|---|---|
| Telegram 게임 지시 접수 | 공동 방장/LeaderBot | 완료 | 포럼 주제 `613`, proposal `b5306d75…` |
| Mini App 승인 | 공동 방장 | 완료 | HTTP 200, approval `cc522f50…` |
| 작업 큐 반영·실행 | 운영 런타임 | 완료 | task `002be156…`, `completion_approval_pending`, gateway outbox `sent` |
| 게임 구현 | 별도 코드 작업자 | 통과 | `star-dodge-game-v2.html` 및 테스트 스크립트 |
| 기능 검증 | 독립 검증자 | 통과(로컬) | 시작·키보드·드래그·충돌·재시작·반응형 |
| 사용자 테스트 URL | 실행 작업자 | 완료 | https://huai-artifacts-117o0lmjl-finder-world.vercel.app/star-dodge-game-v2.html |
| 독립 감사 | 별도 감사 에이전트 | 보완 필요 | 실제 30초는 테스트 훅으로 단축; 공개 URL 클릭 여정은 환경 오류로 미검증 |

### 최종 판정

게임 산출물과 승인-실행-결과반환 흐름은 확인했다. 다만 독립 감사가 지적한 대로 30초 생존은 실제 30초 대기가 아닌 테스트 훅을 사용했고, 공개 URL의 브라우저 클릭 여정은 검증 환경 오류로 완전 확인하지 못했다. 따라서 이번 게임 테스트는 **부분 완료(기능·운영 흐름 통과, 실사용 최종 게이트 보완 필요)**로 기록한다.
