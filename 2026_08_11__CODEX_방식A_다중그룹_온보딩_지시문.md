# Codex 지시문 — 방식 A: 공통 봇 4개로 여러 Telegram 프로젝트방 운영

## 1. 목표

현재 HuAI Collab Chatroom System을 여러 Telegram 그룹에서 그대로 재사용할 수 있게 만든다.

방식은 **A 방식**으로 확정한다.

- Telegram 봇 4개는 공통으로 재사용한다.
- 새 프로젝트마다 봇을 새로 만들지 않는다.
- 새 프로젝트마다 Telegram 비공개 그룹만 새로 만든다.
- 같은 봇 4개를 여러 그룹에 초대한다.
- 중앙 DB는 `telegram_chat_id` 기준으로 room/project를 구분한다.
- 각 room은 서로 다른 owner, members, project_path, gateway 설정을 가질 수 있다.

이 작업은 시험용 MVP가 아니라 **정식 운영 버전 확장 기능**이다.

## 2. 현재 전제

현재 시스템은 다음 기본 구조를 이미 갖고 있다.

- `apps/bot-service`: Telegram webhook, outbox dispatcher, bot role message sender
- `apps/local-gateway`: 로컬 PC에서 Codex/Claude Code 실행
- `packages/orchestrator`: 작업 제안, 승인, 멘션 라우팅, 상태 전이
- `packages/supabase-runtime`: Supabase outbox lease/mark/result ingestion
- `packages/telegram-ui`: Telegram 메시지/버튼 템플릿
- `supabase`: 운영 DB schema/migrations

현재 운영 흐름은 다음 수준까지 구성되어 있다.

```text
Telegram group
→ LeaderBot webhook
→ proposal 생성
→ 방장 승인
→ local-gateway 실행
→ CodexBot 또는 ClaudeBot 결과 보고
→ 의미 있는 결과물일 때만 AuditBot 검증 요청
→ /trace <task_id>로 이벤트·산출물 URI·검증 이력 조회
```

## 3. 확정 운영 방식

### 3.1 공통 봇

아래 역할 봇 4개를 모든 프로젝트방에서 재사용한다.

- LeaderBot: 기본 명령/제안/승인 흐름 표시
- ClaudeBot: Claude Code 실행 결과 표시
- CodexBot: Codex 실행 결과 표시
- AuditBot: 독립 검증 요청/결과 표시

봇 토큰은 프로젝트별로 만들지 않는다.

### 3.2 프로젝트방 구분 기준

모든 inbound Telegram update는 반드시 다음 순서로 판정한다.

1. `telegram_bot_id` 또는 bot username으로 어떤 공통 봇의 webhook인지 식별
2. `telegram_chat_id`로 어떤 room/project인지 식별
3. `telegram_user_id`로 사용자 권한 판정
4. room에 연결된 gateway/project path를 찾음
5. 요청을 해당 room의 작업으로 기록

절대 username, 표시명, 그룹 이름으로 권한이나 room을 판정하지 않는다.

### 3.3 프로젝트별 격리

같은 봇을 쓰더라도 room별로 다음은 분리되어야 한다.

- `room_id`
- `telegram_chat_id`
- `owner_telegram_user_id`
- `room_members`
- `gateway_instances.allowed_project_roots`
- 실행 대상 `projectPath`
- tasks/events/outbox/message_bindings

다른 Telegram 그룹의 요청이 엉뚱한 프로젝트 폴더에서 실행되면 안 된다.

## 4. 만들어야 할 핵심 도구

### 4.1 `scripts/onboard-telegram-room.mjs`

새 Telegram 그룹을 운영 room으로 등록하는 CLI 도구를 만든다.

예상 사용법:

```powershell
node scripts/onboard-telegram-room.mjs `
  --chat-id -100xxxxxxxxxx `
  --owner-id 52485734 `
  --project-path "C:\Dev\CustomerAProject" `
  --title "고객A 프로젝트방"
```

필수 옵션:

- `--chat-id`: Telegram group chat id
- `--owner-id`: 방장 Telegram user id
- `--project-path`: 이 그룹이 실행할 로컬 프로젝트 폴더
- `--title`: room 표시명

선택 옵션:

- `--gateway-id`: 생략 시 현재 기본 gateway 사용 또는 새 gateway row 생성
- `--dry-run`: DB 변경 없이 실행 계획만 출력
- `--json`: 자동화용 JSON 출력

### 4.2 온보딩 스크립트 책임

`onboard-telegram-room.mjs`는 다음을 수행해야 한다.

1. `.env.operation.local` 또는 현재 process env에서 Supabase 운영 env 로드
2. 필수 env 검증
3. `project-path`가 실제 존재하는지 확인
4. 기존 room 중 같은 `telegram_chat_id`가 있는지 확인
5. 없으면 `huai_rooms`에 새 room 생성
6. `huai_room_members`에 owner 등록 또는 갱신
7. 공통 봇 4개에 연결된 active actor/bot 설정 확인
8. `huai_gateway_instances`에 해당 `project-path`를 allowed root로 등록 또는 확장
9. room별 actor/bot/gateway 연결 상태를 검증
10. 결과로 다음을 출력
    - room_id
    - telegram_chat_id
    - owner_telegram_user_id
    - project_path
    - gateway_id
    - 봇 4개 연결 상태
    - 다음 테스트 명령

출력에는 bot token, service role key, webhook secret을 절대 포함하지 않는다.

## 5. DB 처리 원칙

기존 테이블을 우선 재사용한다.

필수로 사용하거나 확장할 테이블:

- `huai_rooms`
- `huai_room_members`
- `huai_ai_actors`
- `huai_telegram_bots`
- `huai_gateway_instances`
- `huai_telegram_updates`
- `huai_events`
- `huai_outbox`
- `huai_tasks`
- `huai_message_bindings`

중복 생성 금지:

- 같은 `telegram_chat_id` room 중복 생성 금지
- 같은 room의 owner 중복 생성 금지
- 같은 room/gateway/project path 중복 등록 금지
- 공통 봇 4개를 project마다 새 token row로 복제 금지

필수 멱등성:

- 스크립트를 여러 번 실행해도 같은 결과가 나와야 한다.
- 이미 등록된 그룹이면 update/upsert로 보정한다.
- 기존 다른 room 설정을 건드리지 않는다.

## 6. Local Gateway 라우팅 요구사항

여러 room이 같은 local-gateway를 공유할 수 있다.

단, 실행 전 반드시 다음을 검증한다.

- executionRequest.roomId가 존재한다.
- room에 연결된 gateway가 존재한다.
- executionRequest.projectPath가 해당 gateway의 `allowed_project_roots` 안에 있다.
- adapterType이 해당 gateway의 `allowed_adapters` 안에 있다.

새 프로젝트 path를 등록할 때는 기존 path를 덮어쓰지 말고 allowed roots에 추가한다.

## 7. Telegram 사용 흐름

새 그룹을 운영에 붙이는 절차는 다음과 같아야 한다.

```text
1. 사람이 Telegram 비공개 그룹 생성
2. 공통 봇 4개 초대
3. 그룹에서 아무 메시지나 보내 chat_id를 확인하거나, 별도 discover 스크립트 사용
4. Codex가 onboard-telegram-room.mjs 실행
5. 방장이 그룹에서 테스트 메시지 전송
6. LeaderBot이 작업 제안 생성
7. 방장이 승인
8. CodexBot 또는 ClaudeBot 실행 결과 보고
9. 의미 있는 결과물일 때만 AuditBot 검증 요청
```

## 8. 명령 라우팅 요구사항

기본 명령:

```text
@leader_chatroom_bot 새 작업 내용
/newtask@leader_chatroom_bot 새 작업 내용
/tasks
/task <task_id>
/search <단어>
/trace <task_id>
```

역할 지정:

```text
@leader_chatroom_bot 클로드 분대장 불러서 이 작업 해
@leader_chatroom_bot Claude Code로 이 작업 해
@leader_chatroom_bot 코덱스로 이 작업 해
@leader_chatroom_bot Codex로 이 작업 해
```

기대 동작:

- Claude/클로드/`@claude_chatroom1_bot` 언급 → `claude_leader` / `claude_code`
- Codex/코덱스/`@codex_chatroom_bot` 언급 → `codex_leader` / `codex`
- 지정이 없으면 기본 실행 actor 사용
- ClaudeBot과 CodexBot을 함께 부르는 요청은 다중 AI 협의 작업으로 정리한다.
- 실행 완료 후 보안, 구현 완료, 문서 작성, DB 변경처럼 의미 있는 결과물일 때만 AuditBot을 호출한다.
- 단순 확인, 짧은 답변, 상태성 응답에는 자동 검증을 붙이지 않는다.

## 9. 보안 원칙

절대 금지:

- bot token 출력
- service role key 출력
- webhook secret 출력
- `.env.operation.local` 내용을 로그에 출력
- Telegram 메시지에 비밀값 포함
- AI prompt에 비밀값 포함
- username/display name으로 권한 판정
- 다른 room의 project path로 실행

허용:

- 비밀값 존재 여부 확인
- key 길이만 출력
- masked error 출력
- room_id/chat_id/project_path 같은 비밀 아닌 운영 식별자 출력

## 10. 검증 요구사항

구현 후 최소 다음 검증을 실행한다.

```powershell
npm run build
npm run verify:orchestrator-owner-flow
npm run verify:supabase-store
npm run verify:local-gateway-consumer
npm run verify:gateway-result-ingestion
npm run verify:telegram-fetch-sender
npm run verify:callback-answer
node scripts/verify-no-secrets.mjs
```

추가로 `onboard-telegram-room.mjs` 테스트를 만든다.

필수 테스트:

- 새 room 생성 계획 생성
- 기존 room 재실행 시 멱등 처리
- project path가 없으면 실패
- 중복 chat id 처리
- owner membership upsert
- gateway allowed roots 확장
- 비밀값 출력 금지

## 11. 운영 완료 조건

다음이 모두 되어야 완료다.

- 공통 봇 4개를 새 Telegram 그룹에 초대해 사용할 수 있다.
- 새 그룹을 CLI 한 번으로 Supabase room에 등록할 수 있다.
- 같은 봇 4개가 여러 그룹에서 동시에 동작한다.
- 각 그룹 요청은 각자 연결된 project path에서만 실행된다.
- owner 권한은 room별로 분리된다.
- 실행 완료 후 의미 있는 결과물일 때만 AuditBot이 검증을 요청한다.
- 사람이 직접 감사를 원하면 `@audit_chatroom_bot`으로 요청할 수 있다.
- `/trace <task_id>`는 이벤트명/시간, 산출물 URI/버전/final 여부, 검증 판정/대상버전을 Telegram에 출력한다.
- `/trace`는 raw event payload와 secret query 값을 출력하지 않는다.
- 기존 운영 그룹의 동작이 회귀하지 않는다.
- secret scan을 통과한다.
- 운영 문서에 새 그룹 추가 절차가 기록된다.

## 12. 구현 순서

1. 현재 schema와 runtime loader에서 room/gateway/actor 관계 확인
2. `onboard-telegram-room.mjs` 설계
3. dry-run 출력부터 구현
4. Supabase upsert 구현
5. gateway allowed roots 확장 구현
6. 온보딩 검증 테스트 작성
7. 기존 verify 세트 실행
8. 운영 서비스 재시작
9. 새 Telegram 그룹으로 실제 연결 테스트
10. 운영 문서 업데이트

## 13. 보고 방식

작업 보고는 다음 형식으로 한다.

```text
완료:
- 무엇을 만들었는지

검증:
- 실행한 검증 명령과 결과

운영 사용법:
- 새 그룹을 붙이는 실제 명령
- Telegram에서 테스트할 문장

주의:
- 사람이 해야 하는 Telegram 그룹 생성/봇 초대 단계
```

추측 보고 금지. 실행하지 않은 검증은 실행하지 않았다고 말한다.
